## Plan

Swap the first two AI cards in `src/routes/hero-preview.tsx` so the layout matches the zapla.io reference:

1. **Left card** becomes "AI-Powered Workflow" with the `aiWorkflowVideo`.
2. **Middle card** becomes "AI Employee" with the `aiEmployeeVideo`.
3. **Right card** (AI Reputation Manager) stays unchanged.

This only touches the `AICard` props and `video src` attributes in the AI section; no other files or styling changes are needed.